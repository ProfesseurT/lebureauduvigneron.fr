---
layout: base.njk
pagination:
  data: annees
  size: 1
  alias: an
permalink: "/outils/calendrier-{{ an }}/"
eleventyComputed:
  title: "Le calendrier du vigneron {{ an }}"
  description: "Une feuille, l'année entière. Obligations, travaux de la vigne, salons et temps forts de vente. À imprimer en A3 et à punaiser au chai. Gratuit, sans compte."
---

{# ============================================================================
   L'ADRESSE DATEE D'UNE ANNEE : /outils/calendrier-AAAA/

   C'est l'adresse CANONIQUE de chaque feuille, celle qui reste, celle qu'un
   moteur indexe, celle qu'on cherche en tapant « calendrier du vigneron 2027 ».
   L'adresse stable /outils/calendrier/ sert la plus recente et pointe ici.

   AJOUTER UNE ANNEE EST UN CHIFFRE dans src/_data/annees.js. Rien d'autre.

   LE CALCUL N'EST PAS REFAIT ICI. Le composant appelle `BdvEcheances.etaler`,
   le MEME moteur que la grille du bureau et que la page publique des
   echeances. Une deuxieme implementation aurait diverge, et la divergence se
   serait vue des mois plus tard, sur une feuille imprimee, chez un client.

   POURQUOI PAS DE GABARIT NU. La coque `base.njk` porte la navigation et le
   pied : c'est ce qui fait de cette page une VRAIE page du site, indexable, et
   pas un PDF perdu. Tout ce qui n'est pas la feuille disparait a l'impression,
   dans le bloc `@media print` de style.css.
============================================================================ #}

<script id="bdvEcheances" type="application/json">
{{ echeances | dump | safe }}
</script>

<section class="section calan-intro">
  <div class="container">
    <p class="section__label">Outil gratuit</p>
    <h1>Le calendrier du vigneron {{ an }}</h1>
    <p class="calan-intro__p">
      L'année entière sur une feuille : ce que la douane attend, ce qui se fait dans la vigne,
      les salons, et les jours où l'on vend. Imprime-la en A3 et punaise-la au chai.
    </p>
    <p>
      <button type="button" class="btn btn--bordeaux" id="calanImprimer">Imprimer la feuille</button>
      <a href="/mon-bureau/" class="btn" id="calanBureau">Le voir dans mon bureau</a>
    </p>
  </div>
</section>

{% include "components/feuille-annee.njk" %}
