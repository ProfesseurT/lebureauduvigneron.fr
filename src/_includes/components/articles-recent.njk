{% if collections.posts | length > 0 %}
<section class="articles-section">
  <div class="container--wide">

    <p class="section__label">Le journal</p>
    <h2 class="section__title">Du concret, du terrain.</h2>

    <div class="articles-grid">
      {% for post in collections.posts | slice(0, 3) %}
      <div class="article-card">
        {# LA CATEGORIE, ET PLUS LE PILIER, 19/09/2026. Meme correctif que dans
           articles-une.njk, et pour la meme raison : `pilier` est l'ancienne
           classification ecartee le 10/09/2026, et ce composant la lisait encore.
           `.article-card__pilier` garde son nom, il vit dans src/css/style.css:1513. #}
        {% if post.data.categorie %}
        <p class="article-card__pilier">{{ post.data.categorie }}</p>
        {% endif %}
        <a href="{{ post.url }}" class="article-card__title">{{ post.data.title }}</a>
        {% if post.data.description %}
        <p class="article-card__excerpt">{{ post.data.description }}</p>
        {% endif %}
        <p class="article-card__meta">{{ post.date | dateReadable }}</p>
      </div>
      {% endfor %}
    </div>

    <div class="articles-cta">
      <a href="/articles/" class="btn btn--bordeaux">→ Tous les articles</a>
    </div>

  </div>
</section>
{% endif %}
